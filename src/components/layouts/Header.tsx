import type { FC } from 'react';
import { Container } from './Container';

export const Header: FC = () => {
  return (
    <header>
      <Container>
        <h1>Header</h1>
      </Container>
    </header>
  );
};
